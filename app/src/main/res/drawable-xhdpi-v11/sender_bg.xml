<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/sender" />
    <stroke
        android:width="3dip"
        android:color="@color/sender" />
    <corners android:radius="3dip" />

</shape>